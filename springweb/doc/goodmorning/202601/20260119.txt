Good morning QUIZ! 오늘도 좋은 하루 되시길~~ ^^

## Ⅰ. 객관식 문제 (4지 선다형)

**1. 게시판 상세 화면에서 다시 리스트 화면으로 돌아갈 때, 이전 검색 조건과 페이지 번호를 유지해야 하는 가장 큰 이유는 무엇입니까?**
① 서버의 메모리 부하를 줄이기 위해서
② 보안을 강화하여 데이터 유출을 막기 위해서
#③ 사용자가 이전에 보던 데이터 맥락을 유지하여 편의성을 높이기 위해서
④ DB의 Index 사용량을 최적화하기 위해서

**2. Controller 클래스 상단에 `@ModelAttribute("sch")`가 선언된 메서드의 특징으로 옳은 것은?**
#① 해당 Controller 내의 모든 요청 메서드 호출 시 마다 모델 데이터가 자동으로 생성되어 전달된다.
② 오직 특정 요청 URL에서만 데이터가 바인딩된다.
③ View단(JSP)에서는 `${sch}` 형식을 사용할 수 없다.
④ 서버가 실행될 때 딱 한 번만 실행되어 상수로 고정된다.

**3. 게시판의 '답글 작성' 처리 시, 원본 글이 있던 페이지 번호 정보를 유지하기 위해 주로 사용하는 방식은?**
① 브라우저의 쿠키(Cookie)에만 저장한다.
#② `<input type="hidden">`을 통해 페이지 번호와 검색 키워드를 전송한다.
	jsp pageSize, curPage, schWriter, schSubject
	controller @ModelAttribute("sch")
③ 무조건 세션(Session)에 모든 정보를 저장하여 관리한다.
④ 서버의 전역 변수(Static)에 페이지 번호를 저장한다.

**4. `BoardSch` DTO에서 `pageSize` 속성이 담당하는 역할은 무엇입니까?**
① 전체 게시물의 총 개수
② 화면 하단에 표시될 페이지 블록의 개수
#③ 한 페이지당 보여줄 게시물의 건수
④ 현재 사용자가 클릭하여 보고 있는 페이지 번호

**5. Spring Boot에서 이메일 발송 기능을 구현하기 위해 `pom.xml`에 추가해야 하는 의존성(Starter)은?**
① spring-boot-starter-web
#② spring-boot-starter-mail
③ spring-boot-starter-validation
④ spring-boot-starter-security

**6. Gmail SMTP 서버를 사용할 때 `spring.mail.port` 설정값으로 가장 적절한 것은? (TLS 기준)**
① 80
② 443
#③ 587
④ 3306

**7. 이메일 설정 중 `spring.mail.properties.mail.smtp.starttls.enable=true`의 의미는?**
① 이메일 발송 시 압축 기능을 활성화한다.
#② 데이터 전송 시 암호화 프로토콜(TLS)을 사용한다.
③ 발신자의 비밀번호를 DB에 자동으로 저장한다.
④ 수신자가 메일을 읽었는지 확인하는 기능을 활성화한다.

**8. Gmail SMTP를 이용해 메일을 보낼 때, 실제 구글 계정 비밀번호 대신 보안을 위해 사용하는 것은?**
① 공인인증서
#② 앱 비밀번호 (App Password)
③ 2단계 인증 OTP 번호
④ AWS Access Key

**9. Spring에서 메일 발송을 실질적으로 처리하는 핵심 인터페이스(객체)는?**
① MailSender
#② JavaMailSender
③ MimeMessageHelper
④ EmailTemplate

**10. 메일 발송 서비스에서 제목, 수신자, 내용을 설정하기 위해 생성하는 객체는?**
#① MimeMessage
② HttpServletRequest
③ ModelAndView
④ StringTokenizer

**11. 다음 중 `EmailRequest` DTO의 속성과 `MimeMessage` 설정 메서드의 매핑이 잘못된 것은?**
① recipient - `setRecipient()`
② subject - `setSubject()`
③ messageBody - `setText()`
$④ fromAddress - `setReplyTo()` (일반적인 단순 발송 기준)
	발신자 설정 setFrom, setReplyTo() 회신받을 주소를 따로 지정할 때

**12. 비밀번호 분실 시 임시 비밀번호를 발송하는 로직의 순서로 가장 적절한 것은?**
① 메일 발송 → 임시번호 생성 → DB 업데이트 → ID 조회
② ID 조회 → DB 업데이트 → 임시번호 생성 → 메일 발송
#③ ID 조회 → 임시번호 생성 → DB 업데이트 → 메일 발송
④ 임시번호 생성 → 메일 발송 → ID 조회 → DB 업데이트

**13. WebSocket 프로토콜의 특징으로 옳은 것은?**
① 클라이언트의 요청이 있을 때만 서버가 응답하는 단방향 통신이다.
② HTTP 프로토콜과 완전히 별개의 포트만을 사용해야 한다.
#③ 서버와 클라이언트 간의 양방향 실시간 통신을 지원한다.
④ 한 번 연결되면 브라우저를 닫아도 영구적으로 연결이 유지된다.

**14. Spring에서 WebSocket 핸들러를 등록하기 위해 구현(implements)해야 하는 인터페이스는?**
① ViewResolver
#② WebSocketConfigurer
③ HandlerInterceptor
④ ConnectionFactory

**15. WebSocket 핸들러 클래스에서 클라이언트가 접속에 성공했을 때 자동으로 호출되는 메서드는?**
① handleTextMessage()  메시지 전송
#② afterConnectionEstablished() 접속
③ afterConnectionClosed() 접속 종료
④ handleTransportError() 예외 발생..

**16. 채팅 서버에서 현재 접속 중인 모든 사용자에게 메시지를 전달(브로드캐스팅)하기 위해 관리해야 하는 객체는?**
① List<String>
#② Set<WebSocketSession>
③ Map<String, Object>
④ HttpServletResponse

**17. WebSocket 연결 시 사용하는 URL 프로토콜 접두어는?**
① http://
② ftp://
#③ ws://
④ jdbc://

**18. 채팅 화면에서 메시지를 보낼 때 JS에서 호출하는 WebSocket 객체의 메서드는?**
① socket.push()
#② socket.send()
③ socket.emit()
④ socket.transfer()

**19. `application.properties`에서 `spring.mail.properties.mail.debug=true` 설정 시 나타나는 현상은?**
① 메일 발송 속도가 빨라진다.
#② 콘솔 창에 메일 전송 과정에 대한 상세 로그가 출력된다.
③ 메일이 실제로 발송되지 않고 테스트 모드로 동작한다.
④ 수신자에게 디버그용 메일이 한 통 더 발송된다.

**20. WebSocket 핸들러에서 메시지를 수신했을 때 실행되는 메서드 내에서, 보낸 사람의 정보를 식별할 수 있는 매개변수는?**
#① WebSocketSession session
② TextMessage message
③ Exception exception
④ CloseStatus status

---

## Ⅱ. 주관식 기술형 문제

**1. 게시판의 상세 화면(`a03_boardDetail.jsp`)에서 '수정'이나 '목록' 버튼을 눌렀을 때, 
	기존의 검색 조건(제목, 작성자)과 현재 페이지 번호가 유실되지 않도록 하기 위한 JSP 태그 구성 방식을 기술하세요.**
	<input type="hidden">`을 통해 페이지 번호와 검색 키워드를 전송한다.
		jsp pageSize, curPage, schWriter, schSubject	

**2. `@ModelAttribute`를 메서드 레벨에서 사용하여 "sch"라는 이름으로 객체를 반환할 때, 이 방식이 개별 `@RequestMapping` 메서드에서 각각 `model.addAttribute()`를 호출하는 방식보다 효율적인 이유는 무엇인지 기술하세요.**
	controller 상단에 공통 모델 어트리뷰트 선언.. @ModelAttribute
	

**3. Gmail SMTP 환경 설정을 위해 `application.properties`에 반드시 포함되어야 할 핵심 속성 5가지를 기술하세요.**
	spring.mail.host=smtp.gmail.com
	spring.mail.port=587
	spring.mail.username=사용자계정(이메일)
	spring.mail.password=앱비밀번호
	spring.mail.properties.mail.smtp.starttls.enable=true




**4. `EmailRequest` DTO를 사용하여 메일을 발송하는 Service 단의 로직에서, `JavaMailSender`와 `MimeMessage`를 
	활용하여 메일을 전송하는 핵심 코드 프로세스를 단계별로 설명하세요.**
	1. 메시지 생성 : javaMailSender.createMimeMessage()  MimeMessage 객체 생성
	2. 헬퍼 생성 : 첨부파일이나 인코딩 처리를 쉽게 하기 위해 new MimeMessageHelper(message, true, "utf-8") 생성
	3. 정보 입력 : heper를 이용해 수신자(setTo), 제목(setSubject), 내용(setText)설정
	4. 발송 : javaMailSender.send(message) 호출..

**5. WebSocket을 이용한 실시간 채팅 구현 시, 서버(Java) 측 핸들러에서 접속한 클라이언트 세션들을 저장하고 관리해야 하는 이유와
	 연결 종료 시(`afterConnectionClosed`) 수행해야 할 필수 작업을 기술하세요.**
	서버에 누가 접속해 있는지 알아야 특정인 혹은 전체에게 메시지를 보낼 수 있다.
	종료시 접속된 세션을 제거 처리..





