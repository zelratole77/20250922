Good morning QUIZ! 오늘도 좋은 하루 되시길~~ ^^

## I. 객관식 (4지선다형, 20문항)

### [PART 1] 클래스와 객체의 기본 구조 (Class & Object)

**1. 클래스(Class)와 객체(Object)의 관계를 가장 잘 설명한 것은 무엇입니까?**
① 클래스는 실제 존재하는 물건이고, 객체는 그 물건의 설계도이다.
#② 클래스는 붕어빵 틀(설계도)이고, 객체는 그 틀에서 만들어진 붕어빵(실체)이다.
③ 하나의 클래스로는 오직 하나의 객체만 생성할 수 있다.
④ 객체가 먼저 생성된 후에, 그 객체를 보고 클래스를 정의한다.

**2. 클래스 내부의 구성 요소 중, 객체의 '상태(State)'나 '데이터'를 저장하는 역할을 하는 것은 무엇입니까?**
① 메서드(Method)
② 생성자(Constructor)
#③ 필드(Field)
④ 모듈(Module)

**3. 파이썬 클래스에서 생성자(`__init__`)의 주된 역할로 가장 적절한 것은 무엇입니까?**
① 객체가 소멸될 때 메모리를 정리한다.
#② 객체가 생성될 때 필드값(초기 상태)을 세팅한다.
③ 클래스 내부의 모든 메서드를 자동으로 한 번씩 실행한다.
④ 외부 모듈을 클래스 내부로 불러온다.

**4. 메서드 정의 시 첫 번째 매개변수로 주로 사용되는 `self`에 대한 설명으로 올바른 것은?**
def 메서드명(se1, 매개변수1, 매개변수2):
	se1.필드명1 = 매개변수1
	
참조변수.메서드명(매개데이터1,매개데이터2)	
① `self`는 파이썬의 예약어이므로 다른 이름으로 바꿀 수 없다. 예약어가 아니지만 통일성 있게 사용.
② `self` 없이 변수를 선언하면(예: `name = "홍길동"`), 그 변수는 클래스 전체에서 공유되는 필드가 된다.
#③ 메서드 내부에서 `self.변수명`을 사용해야만 객체 고유의 필드값에 접근하거나 수정할 수 있다.
④ `self`는 객체 자신이 아니라, 부모 클래스를 가리키는 키워드다.

**5. 다음 중 클래스를 설계할 때 논리적으로 가장 자연스러운 생각의 순서는 무엇입니까?**
① 동작(Method) → 초기화(Init) → 상태(State)
② 초기화(Init) → 동작(Method) → 상태(State)
③ 상태(State) → 동작(Method) → 초기화(Init)
#④ 상태(State) → 초기화(Init) → 동작(Action)
	필드정의 ==> 생성자를 통해서 초기화 ==> 메서드를 통해서 동작 처리..

### [PART 2] 메서드와 로직 활용

**6. 객체의 필드값(데이터)을 보호하거나 안전하게 변경하기 위한 방법으로 가장 적절한 코딩 습관은?**
① 필드 변수를 외부에서 직접 수정하도록(예: `obj.hp = -100`) 놔둔다.
#② 필드 값을 변경하는 메서드를 만들고, 그 안에서 조건문(if)을 통해 유효한 값인지 검사한다.
	파이썬  private 와 동일한 개념  __필드명 사용하면 외부적에서 직접적인 접근을 하지 못한다.
	get/set메서드 정의를 통해서 간접적으로 접근하게 한다.

③ 모든 필드 변수를 전역 변수(Global Variable)로 선언한다.
④ 생성자 외에는 절대 필드 값을 변경하지 못하도록 한다.

**7. 아래 코드에서 `p1.greet()`를 호출했을 때 발생하는 현상에 대한 설명으로 맞는 것은?**

```python
class Person:
    def greet(self):
        msg = "안녕하세요"
        print(msg)

```

① `msg`는 필드 변수이므로 `p1.msg`로 외부에서 접근 가능하다.
#② `msg`는 지역 변수이므로 메서드 실행이 끝나면 사라진다.
	msg는 self 접두어가 붙지 않아 지역변수로 사용해서 출력..
③ `self.msg`라고 쓰지 않았으므로 에러가 발생한다.
④ `greet` 메서드는 `self`를 매개변수로 받지 않았으므로 실행되지 않는다.

**8. 스마트폰 배터리 예제에서, 배터리가 0 미만으로 떨어지는 것을 방지하기 위해 사용된 프로그래밍 요소는?**
① 반복문 (for/while)
② 예외 처리 (try/except)
#③ 조건문 (if/else) : 메서드안에서 유효성 check해서 처리..
④ 상속 (Inheritance)

### [PART 3] 객체 간의 관계 (Relationships)

**9. '1:1 관계(One-to-One)'에 해당하는 예시로 가장 적절한 것은?**
① 하나의 버스(Bus)와 여러 명의 승객(Passenger)
② 하나의 마트(Mart)와 진열된 수많은 상품(Product)
#③ 하나의 로봇(Robot)과 그 로봇에 탑승한 한 명의 파일럿(Pilot)
④ 하나의 학교(School)와 소속된 학생들(Students)

**10. 다음 코드는 어떤 관계를 구현한 것입니까?**
class Pilot:
class Person:	

r1 = Robot()
r1.board(Pilot()) 
r1.board(Person()) 
# self.cockpit = pilot  
: 파이썬에서 데이터유형을 선언하고, 할당하는 하는것이 아니고, 할당 후에 해당 데이터 유형이 결정되기 때문에
: 반드시 Pilot객체할 할당하지 않아도 에러 없이 처리가 된다.
데이터유형 참조변수 = new 객체()  : 자바의 경우 데이터 유형이 결정 후, 저장
데이터유형 참조변수 = 25 :  자바의 경우 데이터 유형이 결정 후, 저장 
자바 최신버젼) var p01 = new Person();
```python
class Robot:
    def board(self, pilot):
        self.cockpit = pilot
```
① 객체 A가 객체 B를 상속받음
#② 객체 A(Robot)가 객체 B(Pilot)를 자신의 변수에 저장하여 포함함 (1:1)
③ 객체 A와 객체 B가 서로 독립적으로 존재하며 아무 관계없음
④ 객체 A가 객체 B의 리스트를 가지고 있음 (1:다)

**11. '1:다 관계(One-to-Many)'를 구현할 때, 부모 객체(예: 마트, 버스)가 
       여러 자식 객체(상품, 승객)를 관리하기 위해 주로 사용하는 데이터 타입은?**
       1:다 관계을 가질려면 다중이 처리되는 List(변경)가능한 객체안에 메서드를 이용해서 추가
       append(객체생성자())
       append(객체생성자())
       append(객체생성자()) :여러개의 객체가 추가하여 처리된다.
       
① 정수형 (Integer)
② 문자열 (String)
#③ 리스트 (List)
④ 튜플 (Tuple)

**12. 마트(Mart) 클래스에 상품(Product) 객체를 추가하는 `add_product` 메서드에서 반드시 
수행해야 할 동작은?**
① `self.products = product` 처럼 변수를 덮어쓴다.
#② `self.products.append(product)` 처럼 리스트에 객체를 추가한다.
	__init__(self):
		self.products = [] : 리스트유형의 객체로 선언되어 있어야지 추가할 수 있는 메서드 append()
		지원되기에 반드시 선언되어 있어야 한다.
		self.products = None ? 리스트객체 생성되지 않았기에 append()메서드 지원이 되지 않는다.
③ `product.name`만 추출하여 문자열로 저장한다.
④ `return product`를 통해 상품을 반환하고 끝낸다.

### [PART 4] 모듈과 패키지 (Modules & Packages)

**13. 파이썬 코드의 구조적 포함 관계를 큰 순서대로 나열한 것은?**
① 모듈 > 패키지 > 프로젝트 > 클래스
#② 프로젝트 > 패키지 > 모듈(.py파일) > 클래스/함수
③ 패키지 > 프로젝트 > 클래스 > 모듈
④ 클래스 > 모듈 > 패키지 > 프로젝트

**14. `import a07_object.a01_pck.a01_echo as se` 구문에서 `as se`의 역할은 무엇입니까?**
#① 해당 모듈의 이름을 `se`라는 별칭(Alias)으로 줄여서 부르겠다.
② `se`라는 함수만 가져오겠다.
③ `se` 폴더 안에 있는 파일을 가져오겠다.
④ 해당 모듈을 `se` 모드로 실행하겠다.

**15. `from 모듈명 import *` 구문의 의미로 올바른 것은?**
① 모듈 내의 함수 중 이름이 `*`로 시작하는 것만 가져온다.
#② 모듈 내의 모든 구성요소(클래스, 함수, 변수)를 가져와서 모듈명 없이 바로 사용한다.
	현재 모듈에 선언된 것과 동일하게 사용할 수 있다. 단, 변수명/함수명/클래스명이 중복이 되지 않아야 한다.
③ 모듈 내의 내용은 가져오지 않고 파일의 위치만 확인한다.
④ 모듈을 가져오되, 실행은 시키지 않는다.

**16. 외부 패키지에 있는 모듈을 불러오기 위해 `sys.path.append(...)`를 사용하는 주된 이유는 무엇입니까?**
#① 파이썬 인터프리터가 모듈을 찾을 수 있도록 검색 경로를 추가하기 위해
② 모듈 파일의 이름을 변경하기 위해
③ 모듈 내부의 코드를 수정하기 위해
④ 시스템의 날짜와 시간을 동기화하기 위해

### [PART 5] 내장 객체 활용 (Built-in Objects)

**17. `math` 모듈의 함수 중, 소수점이 있는 실수를 무조건 정수로 '올림' 처리하는 함수는?**
① `math.floor()`
#② `math.ceil()`
③ `math.round()`
④ `math.abs()`

**18. `math.factorial(5)`의 연산 결과로 올바른 것은? (5!의 값)**
① 10 (5 + 4 + 3 + 2 + 1)
② 15 (5 + 4 + 3 + 2 + 1)
③ 100 (5 * 20)
#④ 120 (5 * 4 * 3 * 2 * 1)

**19. `random` 모듈을 사용하여 1부터 6 사이의 정수(주사위 눈금)를 랜덤하게 얻고자 할 때 사용하는 함수는?**
① `random.choice(1, 6)`  배열에서 특정한 index로 배열의 내용을 호출시 가위 바위 보
② `random.random(1, 6)`
③ `random.randint(1, 6)` 정수범위 안에서 래덤으로 가져올 때.
④ `random.shuffle(1, 6)` 범위안에 내용을 섞어서 구성요소가 중복이 없이 임의로 가져오게 할 때.
	랜덤 확율 동일..
	정규분포(일반적으로 현실적인 분포 형태 성적/키/몸무게)
	데이터별로 확률을 지정해서 처리..


**20. 리스트 `menu = ['짜장', '짬뽕', '우동']`에서 임의의 메뉴 하나를 선택하여 반환해주는 함수는?**
#① `random.choice(menu)`
② `random.shuffle(menu)`
③ `random.randint(menu)`
④ `random.select(menu)`

---

## II. 주관식 (기술형, 5문항)

**1. 클래스 내부 메서드에서 `variable = 10`과 `self.variable = 10`의 차이점을 
	'변수의 생명 주기(수명)'와 '접근 범위' 관점에서 서술하시오.**
	variable = 10 : 해당 메서드 내부에서만 사용되는 지역변수..- 다른 메서드나 외부에서 호출 불가
	self.variable=10 : 전역변수(인스턴스변수)로 클래스내에서 어느 곳에든지 호출가능. 외부에서도
		참조변수.필드명 형식(단, __형식 private가 아닌 경우에만)으로 호출하여 사용가능하다.

**2. 객체지향 프로그래밍에서 `__init__` 메서드가 호출되는 정확한 시점은 언제인지 서술하시오.**
	참조변수 = 생성자()로 객체를 생성하는 시점에 호출되어 처리된다..

**3. 버스(Bus) 클래스와 승객(Passenger) 클래스를 1:다 관계로 구현하려고 합니다. 
	버스 객체가 승객들을 태우기 위해 `__init__` 메서드 내에 반드시 선언해야 하는 데이터 구조(필드)는 무엇이며, 그 이유는 무엇입니까?**
	1:다  즉, 다중객체를 선언하기 위해  리스트형으로 초기화 하여야 한다.
	self.passenger = [] : 반드시.(메모리 생성 + 리스트 유형)

**4. `math.floor(3.9)`와 `math.ceil(3.1)`의 실행 결과를 각각 적고, 
	두 함수의 기능적 차이를 설명하시오.**
	math.floor(3.9) 내림 ==> 3 
	math.ceil(3.1) 올림 ==> 4

**5. 파이썬에서 `import random`을 수행한 후 
	`random.randint(1, 10)`을 호출하는 것과, 
	`from random import randint`를 수행한 후 
	`randint(1, 10)`을 호출하는 것의 차이점(코드 작성 방식의 차이)을 설명하시오.**
	import random 후	random.randint(1, 10) : 이름을 지정해서 사용가능
	from random import randint 후 randint(1, 10) : 이름 지정 없이 사용가능.