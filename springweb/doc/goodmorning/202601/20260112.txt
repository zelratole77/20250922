
Good morning QUIZ! 오늘도 활기찬 한주간 시작 되시길~~ ^^

## I. 객관식 (20문항)

*다음 질문에 대해 가장 적절한 답을 고르세요.*

**1. Spring MVC 패턴에서 사용자가 브라우저를 통해 `/boardList`라는 URL을 요청했을 때, 
	가장 먼저 이 요청을 받아 처리하는 구성 요소는 무엇인가요?**

1. Service (비즈니스 로직)
2. DAO (데이터 접근 객체)
#3. Controller (요청 처리 및 흐름 제어)
4. DTO (데이터 전송 객체)

**2. 다음 중 Spring 프레임워크에서 각 계층(Layer)을 담당하는 클래스를 빈(Bean)으로 등록하기 위해
 사용하는 어노테이션(Annotation)의 연결이 올바르지 않은 것은?**
 	Controller ==> Service ==> Dao(Mapper)
 	Controller <== Service <== Dao(Mapper)
 	

1. 컨트롤러 역할을 하는 클래스 - `@Controller`
2. 비즈니스 로직을 수행하는 서비스 클래스 - `@Service`
3. DB와 연결되어 쿼리를 수행하는 매퍼 인터페이스 - `@Mapper`
#4. 화면에 데이터를 전달하는 모델 객체 - `@Autowired`

**3. Controller에서 비즈니스 로직을 처리하는 Service 객체를, Service에서 DB 처리를 하는 
	DAO 객체를 자동으로 주입받아 사용하기 위해 변수 위에 선언하는 어노테이션은?**

1. `@RequestMapping`
#2. `@Autowired`
3. `@RequestParam`
4. `@ModelAttribute`

**4. `A01_BoardController` 클래스 내에 동일한 URL 매핑(`/boardList`)을 가진 메서드가
 두 개 이상 존재할 경우 발생하는 현상으로 올바른 것은?**

1. 오버로딩에 의해 자동으로 적절한 메서드가 실행된다.
#2. 서버 구동 시 혹은 요청 시 중복 매핑(Ambiguous mapping) 오류가 발생한다.
3. 가장 마지막에 작성된 메서드만 유효하게 작동한다.
4. URL 뒤에 숫자가 자동으로 붙어 구분된다.

**5. 사용자가 Form 태그에 `subject`, `writer` 등의 데이터를 입력하여 전송했을 때, 
Controller의 메서드 매개변수에서 `Board` 클래스와 같은 객체(Command Object)로
 한 번에 받기 위한 조건은 무엇인가요?**

#1. Form 태그의 `name` 속성값과 객체(`Board`)의 멤버 변수(Setter) 이름이 일치해야 한다.
	<input name = "subject"	==> setSubject
	<input name = "writer"  ==> setWriter
2. Form 태그의 `id` 속성값과 객체(`Board`)의 클래스 이름이 일치해야 한다.
3. 모든 입력 데이터는 반드시 `String` 타입이어야만 객체로 받을 수 있다.
4. Controller 메서드 내에서 `request.getParameter()`를 반드시 호출해야 한다.

**6. MyBatis(Mapper)에서 쿼리 문 작성 시, Java 객체(DTO)에서 넘어온 변수 값을 SQL 내에 바인딩(Set)하기 위해 사용하는 문법은?**

1. `${variable}`
#2. `#{variable}`
3. `@[variable]`
4. `&(variable)`

**7. Controller에서 DB 조회 결과를 View(JSP) 화면으로 전달하기 위해 사용하는 객체는 무엇인가요?**

#1. `Model`
2. `Session`
3. `Response`
4. `Cookie`

**8. JSP 화면에서 Controller가 전달한 리스트 형태의 데이터(`boardList`)를 반복문으로 출력하기
 위해 사용하는 JSTL 태그는?**

1. `<c:if>`
2. `<c:choose>`
#3. `<c:forEach>`
4. `<c:set>`

**9. 게시판의 '검색 기능' 구현 시, Service 계층(`A02_BoardService`)에서 수행하는 역할로 
제공된 코드 내용과 일치하는 것은?**

#1. 사용자가 입력한 검색어가 `null`인 경우 빈 문자열(`""`)로 변환하고, 앞뒤에 `%`를 붙여 LIKE 검색을 돕는다.
2. 검색어가 없을 경우 DB 연결을 차단한다.
3. 검색어의 유효성을 검사하여 욕설이 포함되어 있으면 에러 페이지로 보낸다.
4. 검색 결과가 없을 경우 자동으로 더미 데이터를 생성한다.

**10. JSP 파일 내에서 Java의 데이터를 출력하기 위해 사용하는 EL(Expression Language) 표기법으로,
 Controller에서 `d.addAttribute("board", boardObj)`로 저장했을 때 `writer` 값을
  출력하는 방법은?**

1. `<%= board.getWriter() %>`
#2. `${board.writer}` : 백틱사용시는 js에서 사용하는 변수일 때.. 
3. `#{board.writer}`
4. `[board.writer]`

**11. 게시글 등록 시 `no`(게시글 번호) 컬럼에 중복되지 않는 고유한 번호를 자동으로 부여하기 위해
 오라클 DB에서 사용하는 객체는?**

1. Index
2. View
#3. Sequence
4. Trigger

**12. 답변형 게시판의 구조에서, 답글(Reply) 처리를 위해 원본 글과 답글을 그룹화하거나 순서를 제어하기 위해 `Board` 테이블에 추가적으로 필요한 핵심 컬럼은 무엇인가요? (제공된 스키마 기준)**

1. `readcnt` (조회수)
#2. `refno` (상위글 번호)
3. `uptdte` (수정일)
4. `writer` (작성자)

**13. 웹 브라우저에서 서버로 데이터를 전송하는 방식 중, URL 뒤에 파라미터가 노출되지 않고 
HTTP Body에 데이터를 담아 보내며, 주로 글 등록/수정 시 사용하는 방식은?**

1. GET 방식
#2. POST 방식
3. REDIRECT 방식
4. FORWARD 방식

**14. `A03_BoardDao` 인터페이스의 메서드 위에 `@Select`, `@Insert` 등의 어노테이션을 
사용하여 쿼리를 직접 작성하는 방식을 무엇이라 하는가?**

1. XML Mapper 방식	- sql을 따로 xml이라는 파일로 저장 처리한다. 
#2. Annotation Mapper 방식 - @Select()  dao단에서 바로 처리하는 방식..
3. JPA 방식
4. Hibernate 방식

**15. Controller의 메서드가 `return "a09_board/a01_boardList";` 와 같이 문자열을 반환할 때, 이 문자열이 의미하는 것은 무엇인가요?**
뷰단에 접두어 설정
spring.mvc.view.prefix=/WEB-INF/views/
뷰단에 접미어 설정
spring.mvc.view.suffix=.jsp	

/WEB-INF/views/a09_board/a01_boardList.jsp	

1. 클라이언트에게 전송할 텍스트 메시지 내용
#2. 이동하거나 렌더링할 View(JSP) 파일의 경로 및 이름
3. DB에 저장할 테이블 이름
4. 실행할 다음 Controller 메서드의 이름

**16. 다음 중 `Board` DTO(VO) 클래스를 작성할 때 필수로 포함되어야 하는 메서드 혹은 구성 요소가 아닌 것은?**

1. `private` 접근 제어자를 가진 멤버 변수
2. 멤버 변수에 값을 저장하고 읽어오기 위한 `Getter/Setter` 메서드
3. 기본 생성자 및 필요에 따른 매개변수가 있는 생성자
#4. DB 연결을 위한 `Connection` 객체

**17. 사용자가 로그인하지 않은 상태에서 게시글을 수정하거나 삭제하지 못하도록 막기 위해, 서버 측에서 사용자 정보를 유지/관리하는 데 가장 적합한 내장 객체는?**

1. `request`
2. `application`
#3. `session`
4. `pageContext`

**18. 제공된 DB 스키마에서 `BOARDFILE` 테이블이 `BOARD` 테이블과 별도로 분리되어 설계된 이유로 가장 타당한 것은?**

1. DB 용량을 줄이기 위해서
#2. 하나의 게시글에 여러 개의 첨부파일이 존재할 수 있는 1:N 관계를 효율적으로 관리하기 위해서
3. 파일 업로드는 트랜잭션 처리가 불필요하기 때문에
4. 게시글 제목 검색 속도를 높이기 위해서

**19. Controller 코드 중 `boardInsert` 메서드에서 데이터 등록 처리 후 `return "등록 성공";` 이 아니라 View 경로를 반환해야 하는 이유는? (Ajax가 아니라는 가정하에)**

1. 등록 성공 메시지는 View에서 처리할 수 없기 때문이다.
2. Controller는 반드시 void 형이어야 하기 때문이다.
#3. 웹 애플리케이션 흐름 상 처리 후 결과를 보여줄 화면(JSP)이나 리다이렉트 경로가 필요하기 때문이다.
4. DB가 문자열을 리턴 받지 못하기 때문이다.

**20. SQL에서 `WHERE SUBJECT LIKE '%' || #{subject} || '%'`와 같이 검색 조건을 처리할 때, 사용자가 검색어를 입력하지 않아 `#{subject}`가 빈 문자열(`""`)로 들어올 경우 실행되는 쿼리의 결과는?**

1. 아무 데이터도 조회되지 않는다.
2. 에러가 발생한다.
3. 제목에 공백이 포함된 데이터만 조회된다.
#4. `LIKE '%%'`가 되어 모든 데이터(전체 리스트)가 조회된다.

---

## II. 주관식 서술형 (5문항)

*다음 질문에 대해 핵심 내용을 중심으로 간략하게 서술하세요.*

**1. Spring MVC의 요청 처리 흐름 서술**
사용자가 웹 브라우저에서 '게시글 등록 버튼'을 눌러 `/boardInsert` 요청을 보냈을 때, DB에 데이터가 저장되고 다시 목록 화면이 보이기까지의 과정을 **Controller, Service, DAO, DB**의 역할과 데이터 흐름을 중심으로 순서대로 서술하세요.
	controller ==> service ==> dao
	controller <==> service <== dao
**2. 의존성 주입(DI)과 어노테이션**
`A01_BoardController`에서 `A02_BoardService` 객체를 `new` 연산자로 직접 생성하지 
	않고 사용할 수 있는 이유는 무엇인지, `@Autowired`와 스프링 컨테이너(Container)의 개념을 사용하여 설명하세요.
	컨테이너가 @Controller, @Service, @Mapper로 선언된 클래스는 자동으로 객체가 생성되고,
	그 객체들같에 @Autowired라는 키워드를 통해서 연관관계를 설정해주는 것을 말한다.	

**3. 검색 조건 처리 로직 (Service Layer)**
제공된 `A02_BoardService.java`의 `boardList` 메서드를 보면, DAO를 호출하기 전에 
`if(sch.getSubject()==null) ...`과 같은 코드가 있습니다. 이 코드가 왜 필요한지,
 그리고 이 처리를 하지 않고 바로 MyBatis(SQL)로 `NULL` 값이 전달되면 어떤 문제가 
 발생할 수 있는지 서술하세요.
 키워드 검색시, 해당 키워드 검색 문자열을 처리해주는 것을 말한다.
 like '%키워드%'
 초기값이 null ==> "",  각 속성을 "%"+property+"%"로 처리해서 위 같은 키워드 검색이
 가능하게 처리한다.
 
  

**4. Model 객체의 역할**
Controller 메서드의 매개변수로 선언된 `Model d`의 역할은 무엇이며, 
`d.addAttribute("list", listData)` 메서드가 수행하는 기능과 이렇게 저장된 데이터가 
최종적으로 어디에서 어떻게 사용되는지 설명하세요.
==> jsp
<c:forEach var="bd" items="${list}">
	<tr>
		<td>${td.no}</td>
		<td>${td.writer}</td>
		<td>${td.readcnt}</td>

**5. 답변형 게시판의 계층 구조 원리**
일반적인 게시판과 달리 '답변형 게시판'은 원글 밑에 답글이 달리는 구조를 가집니다. 
이를 구현하기 위해 DB 테이블에 `refno`(참조 번호) 컬럼이 필요한 이유와, 답글 등록 시
 이 컬럼에 어떤 값이 저장되어야 논리적인 연결이 가능한지 서술하세요.
no =답글 처리시=> refno로 할당해서 해당 글을 등록할 때, 답글 항목을 설정해서
등록시  고유번호는 sequence 처리, 답글번호는 이전 글의 no를 할당해서 저장된다.
 

