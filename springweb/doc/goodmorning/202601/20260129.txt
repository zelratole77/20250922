Good morning QUIZ! 오늘도 좋은 하루 되시길~~ ^^

### [Part 1] 객관식 4지선다형 (20문제)

**1. 파이썬과 오라클 데이터베이스 연동 과정에서, '식당'에 비유했을 때 '주문을 전달하고 결과(음식)를 받아오는 점원' 역할을 하는 객체는 무엇인가?**
① Connection (접속)
② Cursor (커서)
③ Driver (드라이버)
④ Commit (확정)

**2. `oracledb` 라이브러리를 사용하여 DB 연결 처리를 할 때, 일반적인 프로세스 순서로 가장 올바른 것은?**
① Import -> Cursor 생성 -> Connection 연결 -> Execute -> Close
② Import -> Connection 연결 -> Cursor 생성 -> Execute -> Fetch/Commit -> Close
③ Import -> Execute -> Connection 연결 -> Cursor 생성 -> Close
④ Import -> Connection 연결 -> Execute -> Cursor 생성 -> Fetch/Commit

**3. 소스 코드에서 `oci.init_oracle_client(lib_dir=...)` 함수가 수행하는 역할로 올바른 것은?**
① 데이터베이스 서버의 계정 정보를 초기화한다.
② SQL 문법을 검사하는 파서(Parser)를 실행한다.
③ Oracle Instant Client 경로를 지정하여 Thick 모드를 초기화한다.
④ 연결된 모든 세션을 강제로 종료한다.

**4. `Connection` 객체의 `cursor()` 메서드를 호출했을 때 생성되는 객체의 주요 역할이 아닌 것은?**
① SQL 문장을 DB 서버로 전송 및 실행
② 실행된 SQL의 결과 집합(Result Set)을 메모리에 보관
③ 데이터베이스 서버의 IP와 포트 번호 변경
④ 데이터 인출(Fetch) 수행

**5. 다음 중 SQL 인젝션(SQL Injection) 공격을 방지하고 보안성을 높이기 위해 사용하는 데이터 바인딩 방식으로 가장 적절한 것은?**
① `sql = f"SELECT * FROM EMP WHERE NAME = '{name}'"`
② `sql = "SELECT * FROM EMP WHERE NAME = " + name`
③ `sql = "SELECT * FROM EMP WHERE NAME = :name"`, `cursor.execute(sql, {'name': name})`
④ `sql = "SELECT * FROM EMP WHERE NAME = %s" % name`

**6. `cursor.fetchone()` 메서드를 호출했을 때 반환되는 데이터의 파이썬 자료형은 무엇인가? (데이터가 존재할 경우)**
① List (리스트)
② Tuple (튜플)
③ Dictionary (딕셔너리)
④ Set (집합)

**7. `cursor.fetchall()` 메서드에 대한 설명으로 틀린 것은?**
① 쿼리 실행 결과의 모든 행(Row)을 한꺼번에 가져온다.
② 반환되는 전체 구조는 리스트(List) 형태이다.
③ 리스트 내부의 각 요소는 튜플(Tuple) 형태로 구성된다.
④ 데이터가 많더라도 메모리 사용량에는 전혀 영향을 주지 않는다.

**8. `INSERT`, `UPDATE`, `DELETE`와 같은 DML 구문을 실행한 후, 변경된 데이터를 실제 데이터베이스에 영구적으로 반영하기 위해 반드시 호출해야 하는 메서드는?**
① `con.rollback()`
② `cursor.fetch()`
③ `con.commit()`
④ `con.close()`

**9. 다음 SQL 구문 `SELECT * FROM EMP WHERE SAL BETWEEN :1 AND :2`를 실행할 때, 매개변수로 전달해야 할 파이썬 데이터 타입으로 가장 적절한 것은?**
① 딕셔너리 `{'1': 1000, '2': 3000}`
② 튜플 `(1000, 3000)`
③ 리스트 `[1000, 3000]`
④ 문자열 `"1000, 3000"`

**10. 데이터 조회 시 키워드 검색(`LIKE` 연산자)을 수행할 때, 파이썬 코드에서 `%` 와일드카드를 처리하는 방식으로 제공된 예제 코드의 스타일과 일치하는 것은?**
① SQL문에 직접 쓴다: `... LIKE '%:keyword%'`
② 별도 처리가 필요 없다: `... LIKE :keyword` (입력값 그대로 사용)
③ 파이썬 변수에서 포맷팅한다: `data = f'%{input_val}%'` 후 바인딩
④ SQL문에서 결합한다: `... LIKE '%' || :keyword || '%'`

**11. Python의 `@dataclass`로 정의된 DTO(Data Transfer Object) 객체의 속성들을 딕셔너리 형태로 변환하여 SQL 바인딩에 사용하고자 할 때 사용하는 속성은?**
① `__str__`
② `__init__`
③ `__dict__`
④ `__repr__`

**12. 소스 코드 상에서 `lib_dir_path = r'c:\instantclient_21_20'`와 같이 경로 앞에 `r`을 붙이는 주된 이유는?**
① 읽기 전용(Read-only) 파일임을 명시하기 위해
② 경로 내의 백슬래시(`\`)를 이스케이프 문자가 아닌 문자 그대로 인식하기 위해
③ 상대 경로임을 나타내기 위해
④ 리눅스 경로와 호환되게 하기 위해

**13. 데이터베이스 연결 해제(`close()`)를 수행하는 가장 주된 목적은?**
① SQL 실행 속도를 높이기 위해
② DB 서버 및 클라이언트의 시스템 자원(메모리, 세션 등)을 반환하기 위해
③ 커밋되지 않은 데이터를 강제로 저장하기 위해
④ 다음 쿼리의 자동 실행을 예약하기 위해

**14. 제공된 `INSERT` 처리 로직에서 '데이터 유효성 검사 및 형변환' 단계가 수행하는 역할은?**
① SQL 문장의 문법 오류를 수정한다.
② 입력받은 문자열 데이터를 DB 컬럼 타입(정수, 날짜 등)에 맞게 변환한다.
③ 데이터베이스의 비밀번호를 변경한다.
④ 데이터를 암호화하여 전송한다.

**15. Named Parameter 방식(예: `:name`, `:age`)으로 SQL을 실행할 때, `execute` 메서드의 두 번째 인자로 전달해야 하는 데이터 타입은?**
① Tuple
② List
③ Dictionary
④ String

**16. 다음 중 DB 연결 정보 문자열 `"scott/tiger@localhost:1521/xepdb1"`에 포함되지 않은 정보는?**
① 사용자 계정 (User ID)
② 비밀번호 (Password)
③ 접속하려는 테이블 이름 (Table Name)
④ 서비스 식별자 (Service Name/SID)

**17. `INSERT` 구문을 실행할 때 `cursor.execute()`만 수행하고 `con.commit()`을 수행하지 않고 프로그램을 종료했다. 어떤 결과가 발생하는가?**
① 데이터가 정상적으로 저장된다.
② 데이터가 저장되지 않고 롤백(취소)된다.
③ 프로그램 실행 시 에러가 발생한다.
④ 데이터가 임시 저장소에 남아서 다음 접속 시 보인다.

**18. 제공된 예제에서 `StudentDTO(s_name, s_kor, s_eng, s_math)`와 같이 클래스 객체를 생성하여 데이터를 관리하는 주된 장점은?**
① SQL 실행 속도가 빨라진다.
② 데이터의 구조를 명확히 하고, 관련된 데이터를 하나의 객체로 묶어 관리 및 이동이 용이하다.
③ 데이터베이스 연결 코드를 생략할 수 있다.
④ 오라클 DB가 객체 지향 DB로 자동 변환된다.

**19. `con.connect()` 함수가 실패하여 `con` 객체가 생성되지 않았을 때, 예외 처리를 하지 않는다면 어떤 문제가 발생하는가?**
① 자동으로 재접속을 시도한다.
② `NoneType` 관련 에러 등으로 프로그램이 비정상 종료된다.
③ 기본 계정으로 접속된다.
④ 경고 메시지만 출력하고 다음 코드가 실행된다.

**20. 제공된 코드의 `select_dataPreAll(sql, schDic)` 함수가 내부적으로 호출할 것으로 예상되는 커서의 메서드는? (여러 행을 반환해야 함)**
① `execute()` 후 `fetchone()`
② `execute()` 후 `fetchall()`
③ `executemany()`
④ `fetchmany()`

---

### [Part 2] 주관식 기술형 (5문제)

**1. 데이터베이스 프로그래밍에서 'DB 연결 처리(Connection)' 과정을 식당에 비유하여 설명한 내용이 있습니다. [접속, 커서 생성, 실행, 인출/확정, 종료]의 5단계를 식당을 이용하는 상황(손님, 문, 점원, 주문서 등)에 빗대어 간략히 서술하시오.**

**2. 데이터를 조회할 때 사용하는 `fetchone()` 메서드와 `fetchall()` 메서드의 차이점을 '반환되는 데이터의 형태(자료형)'와 '가져오는 데이터의 양' 측면에서 설명하시오.**

**3. SQL을 작성할 때 변수가 들어갈 자리에 값을 직접 문자열 결합으로 넣지 않고, `:keyword`나 `:1`과 같은 바인딩 변수(Prepared Statement 방식)를 사용하는 가장 중요한 보안상의 이유는 무엇인가?**

**4. Python 프로그램에서 사용자로부터 데이터를 입력받아 DB에 `INSERT` 하는 과정은 [데이터 수집 -> 유효성 검사/형변환 -> SQL 바인딩 -> 실행 -> 확정]의 단계를 거칩니다. 이 중 '확정(Commit)' 단계가 필요한 이유를 설명하시오.**

