Good morning QUIZ! 오늘도 발전하는 하루 되길~~ ^^

## I. 객관식 문항 (20문제)

**1. Ajax(Asynchronous JavaScript and XML)의 가장 핵심적인 특징으로 옳은 것은?**
① 페이지 전체를 새로고침하여 데이터를 갱신한다.
② 서버와 동기식으로 통신하여 데이터가 올 때까지 브라우저가 대기한다.
#③ 웹 페이지의 일부분만 비동기적으로 서버와 통신하여 갱신한다.
④ 서버의 설정 없이 브라우저 단독으로 데이터베이스에 접근한다.

**2. `@ResponseBody` 어노테이션의 주요 역할은 무엇인가?**
① 리턴값을 View(JSP, HTML)의 경로로 인식하게 한다.
#② 리턴되는 데이터를 HTTP 응답 본문(Body)에 직접 전달한다.
③ 클라이언트의 요청 파라미터를 자동으로 객체에 매핑한다.
④ 데이터베이스의 조회 결과를 JSON 파일로 저장한다.

**3. 다음 중 아이디 중복 체크를 구현할 때 SQL문으로 가장 적절한 것은?**
① `INSERT INTO MEMBER (ID) VALUES (#{id})`
#② `SELECT COUNT(*) FROM MEMBER WHERE ID=#{id}`
③ `SELECT * FROM MEMBER WHERE ID LIKE %#{id}%`
④ `DELETE FROM MEMBER WHERE ID=#{id}`

**4. jQuery Ajax 설정 속성 중 서버로 보낼 데이터를 정의하는 속성은?**
① `url`
② `type`   : form method="get/post" 요청값 전달 방식..
#③ `data`
④ `success`

**5. 다음 중 JSON(JavaScript Object Notation) 데이터 형식에 대한 설명으로 틀린 것은?**
① 키(Key)와 값(Value)의 쌍으로 이루어져 있다.
② 자바스크립트 객체 문법과 매우 유사하다.
③ 사람이 읽기 쉽고 기계가 파싱하기 용이하다.
#④ Spring Controller에서 JSON 응답을 위해 반드시 수동으로 String 변환을 해야만 한다.
	jquery Ajax 기준으로 dataType:"json" 설정시, success:function(obj){}
	매개변수로 객체로 받을 수 있다..

**6. MyBatis의 `@Param` 어노테이션은 언제 사용되는가?**
① SQL문의 실행 결과를 객체로 변환할 때
#② 여러 개의 파라미터를 Mapper 인터페이스 메서드에 전달할 때
③ 서비스 클래스를 빈(Bean)으로 등록할 때
④ 컨트롤러의 경로를 설정할 때

**7. 서비스(Service) 계층에서 아이디 중복 여부를 판단할 때, `dao.isValidId(id) > 0`의 의미는?**
#① 입력한 아이디와 일치하는 데이터가 DB에 이미 존재함
	서비스단에서 비지니스 로직이 적용되어 중복여부를 체크하는 부분...
② 입력한 아이디가 글자 수 제한을 초과함
③ DB 연결에 오류가 발생함
④ 중복된 아이디가 없어 가입이 가능함

**8. `ResponseEntity<?>`를 사용하여 컨트롤러에서 리턴할 때의 장점은?**
① JSP 페이지로만 이동할 수 있다.
#② 데이터뿐만 아니라 HTTP 상태 코드(200 OK 등)를 정밀하게 제어할 수 있다.
	.ok()  200 정상일 때, 매개변수로 선언된 DTO/VO json형식의 문자열을 리턴해준다.
③ SQL 쿼리를 자동으로 생성해준다.
④ 별도의 Service 클래스 없이 DB에 접근한다.

**9. JSP 화면에서 `$(document).ready(function(){...})`의 용도는?**
① 서버에 즉시 데이터를 전송한다.
#② HTML 문서의 모든 요소가 로드된 후 스크립트를 실행하도록 보장한다.
③ 외부 라이브러리를 가져온다.
④ 사용자가 버튼을 클릭했을 때만 호출된다.

**10. 맛집 리스트 조회 시 검색어 검색을 위해 Service 단에서 `sch.setName("%"+sch.getName()+"%")` 처리를 하는 이유는?**
① 문자열의 길이를 늘리기 위해
#② SQL의 `LIKE` 연산자를 이용한 부분 일치 검색을 수행하기 위해
③ 데이터베이스 보안(SQL Injection)을 방지하기 위해
④ JSON 데이터 포맷을 맞추기 위해

**11. 다음 중 Ajax 통신 성공 시 실행되는 콜백 함수는?**
① `error`
② `complete`
#③ `success`
④ `beforeSend`

**12. 아이돌 그룹 정보 조회 예제에서 입력 후 `Enter` 키를 눌렀을 때 이벤트를 발생시키기 위한 `keyCode` 값은?**
① 8
#② 13   if(event.keyCode === 13 )     === 자바스크립트에서는 자동으로 형변환해주기에 데이터유형과 데이터를 동시에 비교해서
										동일할 때, 처리해준다..
③ 27
④ 32

**13. MyBatis 설정에서 `@Select` 어노테이션 내부에 들어갈 내용으로 올바른 것은?**
① 자바 메서드 소스 코드
② 데이터베이스 테이블 생성 구문
#③ 실행하고자 하는 SQL 쿼리문
④ 컨트롤러의 매핑 주소

**14. Ajax 요청 시 `dataType: "json"`을 설정하는 이유는 무엇인가?**
#① 서버에서 보내온 응답 데이터를 JSON 객체로 자동 파싱하기 위해     서버에서 가져오는 데이터는 문자열이기에 이것을 객체로 변환..
② 서버로 보낼 데이터 형식을 JSON으로 지정하기 위해
③ 자바스크립트 변수 타입을 강제로 변환하기 위해
④ 데이터베이스의 컬럼명을 JSON으로 변경하기 위해

**15. `@RestController`와 `@Controller`의 주된 차이점은?**
	비동기 통신만을 위한 controller 여기서는 모든 데이터 처리는 json형식으로만 처리할 경우 사용된다..
#① `@RestController`는 모든 메서드에 `@ResponseBody`가 기본적으로 적용된다.
② `@Controller`는 비동기 통신을 지원하지 않는다.
③ `@RestController`는 JSP 파일만 리턴할 수 있다.
④ 두 어노테이션은 기능상 아무런 차이가 없다.

**16. 다음 중 비동기 통신 시 화면의 깜빡임(새로고침)이 없는 이유는 무엇인가?**
① 브라우저의 캐시 기능을 이용하기 때문
#② 전체 HTML을 다시 받는 것이 아니라 필요한 데이터만 주고받기 때문
③ 고속 인터넷 전용 기술이기 때문
④ 서버에서 브라우저의 엔진을 직접 제어하기 때문

**17. 검색 조건 객체(DTO)에서 값이 `null`일 때 빈 문자열(`""`)로 치환해주는 로직을 작성하는 가장 적절한 위치는?**
① JSP (View)
② Controller
#③ Service          초기화면에서 입력값이 없을 때, 키워드 검색(%%)시 전체 데이터를 출력하기 위하여 사용된다.
④ Dao (Mapper)

**18. 맛집 관리 예제에서 모달(Modal) 창의 주된 용도는 무엇인가?**
① 새로운 웹 페이지로 이동하기 위해
#② 현재 화면 위로 팝업을 띄워 상세 정보 확인 및 입력을 처리하기 위해

③ 서버의 데이터를 삭제하기 위한 전용 도구
④ 광고를 띄우기 위한 용도

**19. jQuery를 사용하여 input 태그의 값을 가져오는 코드로 옳은 것은?**
① `$("input").text()`
#② `$("input[속성=속성값]").val()`
③ `$("input").html()`
④ `$("input").getAttribute()`

**20. MVC 패턴에서 DAO(Data Access Object)의 역할은?**
① 클라이언트의 요청 처리
② 비즈니스 로직 수행
#③ 데이터베이스와의 직접적인 통신 및 SQL 실행
④ 사용자에게 보여줄 결과 화면 생성

---

## II. 주관식 기술형 문항 (5문제)

**1. Ajax 통신 시 클라이언트 측에서 서버로 데이터를 전달할 때 사용하는 `data` 속성과 서버로부터 받은 결과값이 들어오는 
	`success` 함수의 매개변수 간의 데이터 흐름에 대해 기술하세요.**
	
	url, data(요청값) 속성을 통해 서버를 호출  @GetMapping(url)  @RequestParam("key") 
	return ResponseEntity.ok(전달할 데이터-객체);  json 데이터로 변화
	success:function(서버에서 받은 객체 변수){
	
	}
	

**2. 회원가입 시 아이디 중복 체크 로직에서 Controller가 문자열(String)을 리턴하고, 
JSP의 `success: function(msg)`에서 이를 처리하는 과정의 핵심 메커니즘을 설명하세요.**
	@ResponseBody json변환이 필요없는 문자열을 전달 할 때 주로 사용
	return "전달할 메시지"
	

**3. MyBatis 환경에서 인터페이스 기반의 DAO와 `@Select` 어노테이션을 사용할 때, 쿼리문 내의 
	`#{id}`와 같은 파라미터 바인딩 방식이 기존 JDBC 방식과 비교하여 가지는 보안적 장점을 기술하세요.**
	SQL Injection이라는 sql 구문을 이용해서  요청값으로 데이터를 가져오는 문제를 Mybatis는 #{id}식으로
	처리하여 내부적으로 Prepared Statement를 처리함으로 보안적인 문제를 해결하였다.

**4. 맛집 리스트 검색 기능 구현 시 Service 단에서 검색 키워드 양옆에 `%`를 붙여주는 가공 처리를 하는 이유와 
	이것이 SQL 쿼리(LIKE 연산자)에 미치는 영향에 대해 기술하세요.**
	Service단에서 %데이터%를 요청데이터로 전달함으로 키워드 검색이 가능하게 처리하였다. - 검색의 편의성을 높여준다.

**5. 아이돌 정보 조회 예제에서 `dataType: "json"`으로 응답받은 후, JavaScript의 템플릿 리터럴(백틱 ` ` )과
	 `${group.groupName}` 형식을 사용하여 동적으로 HTML 테이블 행(`<tr>`)을 생성하는 과정의 이점을 기술하세요.**
	 json데이터를  객체로 받아서 해당 화면의 DOM객체에 html테그를 백틱과 ${}형식으로 처리하여 동적인 화면을 구성할 수 있다.
	 
	 
	 
	 
	 
	 
	 

