Good morning QUIZ! 오늘도 좋은 하루 되시길~~ ^^

### I. 객관식 (4지선다형, 20문제)

**1. 세션(Session) 기반의 로그인 처리 프로세스 중 가장 적절하지 않은 것은?**
① 사용자가 직접 URL로 접근 시 세션 정보가 없으면 로그인 페이지로 리다이렉트한다.
#② 로그인 성공 시 사용자의 주요 정보는 클라이언트의 쿠키에만 저장하여 관리한다.
③ 게시물 등록 시 세션에 저장된 정보를 호출하여 작성자 ID를 자동으로 할당할 수 있다.
④ 상세 화면에서는 세션의 ID와 게시글의 작성자 ID를 비교하여 권한을 확인한다.

**2. Spring Controller에서 로그인을 처리할 때, DB에서 가져온 `Member` 
	객체가 `null`이 아닐 경우 수행해야 할 작업으로 옳은 것은?**
#① `HttpSession` 객체에 `setAttribute()`를 사용하여 회원 정보를 저장한다.
② `Model` 객체에 회원 정보를 담고 브라우저를 강제 종료한다.
③ `HttpServletRequest`를 초기화하고 로그인 페이지로 포워딩한다.
④ 즉시 DB의 해당 레코드를 삭제하여 보안을 강화한다.

**3. JSP 페이지 상단에서 공통적으로 처리하는 세션 체크 로직의 목적으로 올바른 것은?**
① 페이지의 로딩 속도를 향상시키기 위해
#② 비로그인 사용자의 부적절한 서비스 접근을 차단하기 위해
③ 데이터베이스의 커넥션 풀을 관리하기 위해
④ HTML 태그의 문법 오류를 검사하기 위해

**4. 상세 페이지에서 '수정' 및 '삭제' 버튼의 노출 여부를 결정하는 jQuery 로직으로 가장 적절한 조건문은?**
① `if( sessId == writer && sessAuth == "admin" )`
② `if( sessId != writer || sessAuth == "admin" )`
#③ `if( sessId == writer || sessAuth == "admin" )` 등록된 작성자 id와 세션 id가 같거나 세션권한이 관리자 일때는
													수정/삭제가 가능
④ `if( sessId == writer )` (단, 관리자 권한은 무시함)

**5. 게시글 수정/삭제 버튼을 권한에 따라 제어할 때 사용하는 jQuery 함수 쌍으로 옳은 것은?**
① `push()` / `pop()`
② `append()` / `remove()`
#③ `show()` / `hide()`
④ `addClass()` / `removeClass()`

**6. 답글(Reply) 기능 구현 시, 부모 글의 번호를 저장하기 위해 사용하는 컬럼명으로 본문에서 제시된 명칭은?**
① `no`
#② `refno`   답글 클릭시, controller단에서 현재 글의 번호를 답글번호로 할당 처리..
③ `subject`
④ `cnt`

**7. 답글 작성 화면으로 이동할 때, 제목(Subject) 처리 로직으로 가장 적절한 것은?**
① 기존 제목을 삭제하고 공백으로 전달한다.
② 기존 제목 뒤에 " (Copy)"를 붙여 전달한다.
#③ 기존 제목 앞에 "RE:"를 붙여 전달한다.
④ 제목은 수정할 수 없도록 고정한다.

**8. 답글 처리 시 Controller에서 `Board` 모델 객체의 `content` 필드에 "=========== 이전글 ===========" 문자열을 추가하는 주된 이유는?**
① 데이터베이스의 용량을 늘리기 위해
#② 사용자에게 이전 대화 맥락을 유지하여 편의성을 제공하기 위해
③ SQL의 JOIN 연산을 대체하기 위해
④ 작성자의 아이디를 암호화하기 위해

**9. 계층형 SQL에서 '계층의 깊이'를 나타내는 가상 컬럼은 무엇인가?**
#① `LEVEL`
② `DEPTH`
③ `STEP`
④ `RANK`

**10. 계층형 쿼리에서 최상위 노드(부모가 없는 노드)를 지정하기 위해 사용하는 예약어는?**
			  최상위              1
		차상위		차상위		2
	차하위	차하위	차하위  		3


① `ORDER SIBLINGS BY`
② `CONNECT BY PRIOR`
#③ `START WITH`
④ `GROUP BY`

**11. 계층형 쿼리에서 부모 행과 자식 행의 관계를 연결할 때 사용하는 구문은?**
① `START WITH NO = 0`
#② `CONNECT BY PRIOR NO = REFNO`
③ `JOIN ON NO = REFNO`
④ `WHERE NO = REFNO`

**12. 계층형 쿼리 결과 내에서 같은 부모를 가진 자식 노드들끼리의 정렬을 유지하기 위해 사용하는 구문은?**
① `ORDER BY`
② `GROUP BY`
#③ `ORDER SIBLINGS BY` 게시물의 경우 최근에 등록한 글이나 최근에 답한 글을 가장 위로 정렬하기에 컬럼명 desc 를 주로 사용한다.
④ `SORT BY`

**13. 게시판 페이징(Paging) 처리가 필요한 가장 핵심적인 이유는?**
① 화면 디자인을 화려하게 만들기 위해
#② 대량의 데이터 로딩 시 발생하는 시스템 부하를 방지하고 성능을 최적화하기 위해
③ 사용자가 글을 수정하지 못하게 하기 위해
④ 서버의 저장 공간을 절약하기 위해

**14. 페이징 알고리즘의 시작점이 되는 데이터는 무엇인가?**
① 현재 페이지 번호
② 한 페이지당 보여줄 건수
#③ DB에 저장된 전체 데이터 총건수(Count)
④ 마지막 페이지 번호

**15. 총 데이터가 53건이고, 한 페이지에 10건씩 보여주기로 설정했을 때 계산되는 '총 페이지 수'는?**
① 5페이지
#② 6페이지
③ 5.3페이지
④ 10페이지

**16. SQL에서 특정 범위의 데이터만 추출하기 위해 `ROWNUM`과 함께 사용되는 연산자는?**
① `LIKE`
#② `BETWEEN`
③ `IN`
④ `EXISTS`

**17. `BoardSch` DTO(또는 모델 객체)에서 페이징 처리를 위해 반드시 관리해야 할 정보가 아닌 것은?**
① `curPage` (현재 클릭한 페이지 번호)
② `pageSize` (한 페이지당 보여줄 데이터 건수)
③ `start` / `end` (SQL에서 사용할 시작/종료 번호)
#④ `databaseName` (접속한 데이터베이스의 이름)

**18. 2페이지를 호출할 때, 한 페이지당 10건씩 출력한다면 SQL의 `BETWEEN` 조건에 들어갈 값으로 옳은 것은?**
① `BETWEEN 1 AND 10`
#② `BETWEEN 11 AND 20`
③ `BETWEEN 21 AND 30`
④ `BETWEEN 2 AND 20`

**19. 페이징 처리 시 사용자가 페이지 번호를 클릭하면 Controller로 전달되어야 하는 가장 중요한 파라미터는?**
① `refno`
② `writer`
#③ `curPage`  현재페이지 번호를 클릭시, 한번에 보여줄 페이지 갯수와 함께   start, end를 도출하는 알고리즘 구현..
④ `level`

**20. 본문 내용 중, 답글 등록 시 `a02_boardInsert.jsp` 화면에서 `refno` 값이 없을 경우(새 글일 경우) 기본값으로 설정하는 수치는?**
① 1
② -1
#③ 0
④ NULL

---

### II. 주관식 기술형 (5문제)

**1. 게시판 조회 시 세션 정보가 없는 경우 발생해야 하는 로직의 흐름을 '메시지 출력', '페이지 이동'의 관점에서 기술하세요.**
	세션 정보가 없을 때, alert()으로 로그인하셔야 합니다 로딩 후, login 페이지로 이동 처리

**2. 계층형 SQL 작성 시 `ORDER BY` 대신 `ORDER SIBLINGS BY`를 사용해야 하는 이유를 계층 구조 유지의 관점에서 설명하세요.**
	계증 구조에서 일단 기본적으로 조회(최상위)되는 것의  정렬을 한 후에, 답글마다 해당 글의 하위에 다시 정렬 정리해야기에
    order siblings by를 사용한다.

**3. 페이징 처리 알고리즘에서 `총 페이지 수`를 구하는 공식을 '총 데이터 건수'와 '페이지당 데이터 건수'를 이용하여 설명하세요.**
	총페이지수는 나눗셈에서 나머지값이 있을 때는 하나더 올려주는 올림처리가 필요하다.
	Math.ceil(총데이터건수/총페이지수) : 올림 처리..
**4. 게시판 상세 화면에서 작성자 본인과 관리자(admin)에게만 수정/삭제 버튼이 보이도록 처리하는 클라이언트 측 로직을 간단히 서술하세요.**
 	서버단에서 설정한 session mem으로 객체형 데이터를 설정해서
 	mem.id==화면에작성자id 로 처리하여 동일한지 (즉, 현재 로그인한 사용자가 글의 작성한 사람인지 여부 확인)
 	mem.auth == 'admin'  현재 사용자의 권한이 관리자 권한인지 여부 확인
 	이 둘 중에 하나라도 해당하면 수정/삭제 버틑이 보이도록 처리..

**5. 답글(Reply) 등록을 위해 상세 화면에서 등록 화면으로 넘어갈 때, 기존 게시물의 데이터(제목, 내용 등)를 
	어떻게 가공하여 전달하는지 그 과정을 기술하세요.**
	부모 글의 no를 refno에 할당하고, 제목(subject)에 RE:를 추가하며,
	내용 content에 구분선과 원본 내용을 결합하여 등록 화면에 전달..
	
	
	
	
	
	
	
	

