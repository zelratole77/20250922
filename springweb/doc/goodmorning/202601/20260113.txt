Good morning QUIZ! 오늘도 좋은 하루 되시길~~ ^^

## I. 객관식 문항 (20문제)

**1. 다음 중 `A02_BoardService`의 `boardList` 메서드에서 검색어(`Subject`, `Writer`)에 `%`를 붙여 설정하는 주된 이유는 무엇입니까?**
① SQL의 `IN` 연산자를 사용하기 위해
② MyBatis의 동적 SQL에서 `#{}` 대신 `${}`를 강제하기 위해
③ SQL `LIKE` 연산자를 통한 부분 일치 검색을 수행하기 위해
④ 데이터베이스의 인덱스 성능을 극대화하기 위해

**2. `boardUpdateAjax` 메서드에서 `@RequestBody Board upt`를 사용하는 이유로 가장 적절한 것은 무엇입니까?**
① 클라이언트가 `application/x-www-form-urlencoded` 형식으로 데이터를 보냈기 때문에
② HTTP 요청 본문(Body)에 담긴 JSON 문자열을 Java 객체로 변환하기 위해
③ URL 쿼리 스트링에 포함된 파라미터를 읽어오기 위해
④ 서버의 세션에 데이터를 저장하기 위해

**3. jQuery AJAX 코드 중 `data: $("#frm02").serialize()`가 수행하는 역할은 무엇입니까?**
① 폼 내의 모든 입력 요소를 JSON 객체로 변환한다.
② 폼 내의 name과 value를 `key=value&key=value` 형태의 쿼리 스트링으로 변환한다.
③ 폼 데이터를 서버로 보내기 전 유효성 검사(Validation)를 수행한다.
④ 서버에서 받은 응답 데이터를 HTML 테이블로 렌더링한다.

**4. `boardUpdateAjax` 호출 시 `contentType: "application/json"`을 설정하는 이유는 무엇입니까?**
① 서버로부터 JSON 타입의 응답을 받겠다고 명시하기 위해
② 서버에 전송하는 데이터의 형식이 JSON임을 알리기 위해
③ 브라우저의 캐시 기능을 끄기 위해
④ 파일 업로드를 위한 멀티파트 형식을 지정하기 위해

**5. `A03_BoardDao` 인터페이스의 `@Update` SQL 문에서 `UPTDTE = SYSDATE`의 의미는 무엇입니까?**
① 게시물이 처음 작성된 시간을 기록한다.
② 게시물이 수정된 시점의 현재 서버 시간을 기록한다.
③ 다음 게시물 등록 시간을 예약한다.
④ 조회수가 증가한 시간을 별도로 저장한다.

**6. `boardDetailAjax` 메서드 실행 시, 서비스 레이어에서 `dao.uptReadCnt(no)`를 호출하는 시점과 목적은 무엇입니까?**
① 목록을 출력할 때마다 모든 게시물의 조회수를 올리기 위해
② 상세 화면을 요청했을 때 해당 게시물의 조회수를 1 증가시키기 위해
③ 게시물이 수정될 때만 조회수를 초기화하기 위해
④ 관리자가 로그인했을 때만 조회 기록을 남기기 위해

**7. JavaScript의 `JSON.stringify(formData)` 함수가 수행하는 작업은 무엇입니까?**
① 문자열 데이터를 자바스크립트 객체로 파싱한다.
② 자바스크립트 객체를 JSON 형식의 문자열로 직렬화한다.
③ 폼 데이터를 HTML 엔티티로 인코딩한다.
④ 서버의 응답을 가로채서 로깅한다.

**8. 다음 중 `boardDeleteAjax` 호출 시 사용된 HTTP 메서드 타입으로 가장 적절한 것은 무엇입니까?**
① GET
② POST
③ PUT
④ DELETE

**9. Controller에서 `ResponseEntity<?>`를 반환 타입으로 사용하는 이점으로 보기 어려운 것은 무엇입니까?**
① 응답 데이터와 함께 HTTP 상태 코드(예: 200 OK)를 제어할 수 있다.
② 별도의 View(JSP) 경로를 반환하지 않고 직접 데이터를 전달할 수 있다.
③ 클라이언트의 요청 방식(GET/POST)을 강제로 하나로 통일시킨다.
④ JSON이나 XML 등 다양한 데이터 형식을 유연하게 응답할 수 있다.

**10. `ajaxList()` 함수 내에서 `$(bList).each(function(idx, bd) { ... })` 구문의 역할은 무엇입니까?**
① 서버에서 전달받은 게시글 리스트를 순회하며 동적으로 HTML 행(tr)을 생성한다.
② 서버에 저장된 전체 데이터 개수를 출력한다.
③ 특정 게시글을 더블클릭했을 때의 이벤트를 등록한다.
④ 입력 폼에 있는 모든 값을 초기화한다.

**11. `goDetail(noVal)` 함수 내에서 `$("#regLoadBtn").click()`을 호출하는 의도는 무엇입니까?**
① 페이지를 새로고침하기 위해
② 등록 폼의 초기 설정(버튼 숨김/표시 등) 로직을 재사용하기 위해
③ 상세 조회를 취소하고 목록으로 돌아가기 위해
④ 서버에 데이터를 중복으로 저장하기 위해

**12. MyBatis에서 `@Param("no") int no` 어노테이션을 사용하는 주된 이유는 무엇입니까?**
① SQL 문 안에서 `#{no}`와 같이 매개변수 이름을 명확하게 지정하기 위해
② 해당 변수를 세션 영역에 저장하기 위해
③ 데이터베이스의 특정 컬럼에 인덱스를 생성하기 위해
④ 숫자형 데이터를 자동으로 문자열로 변환하기 위해

**13. `ajaxList()` 함수에서 서버 응답 성공 시 `$("#showList").html(showList)`를 사용하는 이유는 무엇입니까?**
① 기존 리스트 내용 뒤에 새로운 내용을 추가(append)하기 위해
② 특정 ID를 가진 요소의 내부 HTML을 생성된 `showList` 문자열로 교체하기 위해
③ 사용자가 입력한 검색어를 화면에서 지우기 위해
④ 브라우저의 로컬 스토리지에 데이터를 저장하기 위해

**14. 게시판 수정 로직인 `boardUpdate` 메서드에서 수정 후 다시 `getBoard`를 호출하여 Model에 담는 이유는 무엇입니까?**
① 수정된 최신 데이터가 반영된 상세 페이지를 다시 보여주기 위해
② 데이터베이스와의 연결을 끊기 위해
③ 수정 전의 이전 데이터를 백업하기 위해
④ 조회수를 다시 0으로 초기화하기 위해

**15. `keyup` 이벤트 핸들러에서 `if(event.keyCode==13)`을 체크하는 목적은 무엇입니까?**
① 사용자가 'ESC' 키를 눌렀는지 확인하기 위해
② 사용자가 'Enter' 키를 눌렀을 때 검색 기능을 실행하기 위해
③ 입력 필드에 특수문자가 들어왔는지 검사하기 위해
④ 입력 글자 수가 13자를 넘었는지 확인하기 위해

**16. 서비스 레이어의 `boardList` 메서드에서 `sch.getSubject() == null`일 때 빈 문자열(`""`)을 세팅하는 이유는 무엇입니까?**
① 데이터베이스 용량을 절약하기 위해
② SQL `LIKE` 연산 시 `null`로 인한 오류를 방지하고 전체 검색이 가능하게 하기 위해
③ 클라이언트에게 에러 메시지를 전달하기 위해
④ 게시판의 제목을 "제목 없음"으로 강제 변경하기 위해

**17. `boardInsertAjax` 통신 성공 후 `$("#frm02")[0].reset()`을 호출하는 이유는 무엇입니까?**
① 서버의 데이터베이스를 초기화하기 위해
② 입력 폼에 작성했던 텍스트들을 모두 지워 다음 입력을 준비하기 위해
③ 페이지를 강제로 이전 페이지로 이동시키기 위해
④ 자바스크립트의 모든 전역 변수를 삭제하기 위해

**18. Controller의 `@GetMapping("/boardDetailAjax")`와 `@PostMapping("/boardInsertAjax")`의 차이점에 대한 설명으로 옳은 것은?**
① `GetMapping`은 데이터를 수정할 때 사용하고, `PostMapping`은 조회할 때 사용한다.
② `GetMapping`은 보안이 필요한 대용량 데이터 전송에 적합하다.
③ HTTP 메서드 규약에 따라 조회를 위한 요청은 `GET`, 리소스 생성을 위한 요청은 `POST`를 사용한다.
④ 두 어노테이션은 기능적으로 완전히 동일하여 서로 바꾸어 써도 무방하다.

**19. `goDetail` 함수에서 게시글의 날짜를 처리하는 `let regDt = new Date(bd.regdte)` 코드의 역할은 무엇입니까?**
① 서버에서 받은 타임스탬프 또는 문자열 날짜 데이터를 JavaScript Date 객체로 변환한다.
② 현재 시스템의 날짜를 서버로 전송한다.
③ 데이터베이스의 날짜 형식을 변경한다.
④ 게시글의 수정 가능 시간을 제한한다.

**20. `showList` 변수를 생성할 때 사용된 백틱(`)과 `${bd.no}`와 같은 문법의 명칭은 무엇입니까?**
① 화살표 함수(Arrow Function)
② 템플릿 리터럴(Template Literal)
③ 구조 분해 할당(Destructuring Assignment)
④ 프로토타입(Prototype)

---

## II. 주관식 기술형 문항 (5문제)

**1. 게시글 수정 기능에서 `$.each($("#frm02").serializeArray(), function() { formData[this.name] = this.value })` 코드가 수행하는 논리적 절차를 설명하시오.**

**2. Spring Controller에서 `ResponseEntity<?>`를 활용하여 AJAX 응답을 보낼 때, `ResponseEntity.ok(service.boardList(sch))` 코드가 클라이언트(브라우저)에게 전달하는 데이터의 형식과 HTTP 상태 코드의 의미를 기술하시오.**

**3. 게시판 상세 조회(`boardDetail`) 시, `service` 레이어에서 `uptReadCnt`와 `getBoard` 두 가지 메서드를 순차적으로 호출하는 이유와 그 비즈니스 로직의 흐름을 설명하시오.**

**4. 수정(Update) 시에는 `PUT` 방식을 사용하고 `JSON.stringify()`를 통해 데이터를 전송하는 반면, 삭제(Delete) 시에는 `DELETE` 방식을 사용하면서 `data: {no: ...}`와 같이 객체 형태로 전송하고 있습니다. 이 두 방식의 전송 데이터 형식(Content-Type) 차이점을 기술하시오.**

**5. `ajaxList()` 함수 내의 `showList` 생성 코드에서 `ondblclick="goDetail(${bd.no})"`가 포함되어 있습니다. 사용자가 게시글 행(tr)을 더블클릭했을 때 발생하는 일련의 과정을 자바스크립트 함수 호출과 서버 통신 관점에서 설명하시오.**

