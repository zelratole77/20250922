Good morning QUIZ!  활기찬 한주간 시작입니다. ^^

## I. 객관식 사지선다 (20문제)

**1. `@ResponseBody` 어노테이션의 주요 역할로 가장 적절한 것은?**
① 클라이언트의 요청 파라미터를 자바 객체로 변환한다.
② 메서드의 리턴 값을 HTTP 응답 본문(Body)에 직접 기록한다.
③ ViewResolver를 통해 특정 JSP 페이지를 호출한다.
④ 서버의 세션 정보를 클라이언트에 전달한다.

**2. Ajax 통신 시 `dataType: "json"` 설정의 의미는 무엇인가?**
① 클라이언트가 서버로 보낼 데이터의 형식을 지정한다.
② 서버에서 보내온 문자열 데이터를 자바스크립트 객체로 자동 파싱한다.
③ HTML 태그 형태로 데이터를 받겠다는 의미이다.
④ 비동기 방식이 아닌 동기 방식으로 통신하겠다는 설정이다.

**3. `ResponseEntity<T>`를 사용하는 가장 큰 장점은 무엇인가?**
① 별도의 View 페이지가 필요 없다.
② 반환할 데이터와 함께 HTTP 상태 코드(Status Code)를 세밀하게 제어할 수 있다.
③ 오직 JSON 데이터만 전송할 수 있도록 제한한다.
④ SQL 쿼리문을 직접 클라이언트에 전달할 수 있다.

**4. `ResponseEntity.ok(person)` 코드에서 `.ok()`가 의미하는 HTTP 상태 코드는?**
① 404 (Not Found)
② 500 (Internal Server Error)
③ 200 (OK)
④ 302 (Found)

**5. 다음 중 Ajax의 `success` 함수가 실행되는 시점은?**
① 서버에 요청을 보낸 직후
② 서버에서 에러가 발생하여 400번대 코드를 반환했을 때
③ 서버로부터 정상적인 응답이 도착했을 때
④ 브라우저가 새로고침 될 때

**6. Controller에서 문자열을 리턴할 때 `@ResponseBody`가 없다면 Spring은 이를 어떻게 처리하는가?**
① 해당 문자열을 데이터로 인식하여 브라우저에 출력한다.
② 해당 문자열의 이름을 가진 View(JSP 등)를 찾는다.
③ 자동으로 JSON 객체로 변환한다.
④ 실행 시 즉시 컴파일 에러를 발생시킨다.

**7. jQuery Ajax 속성 중 서버로 보낼 데이터를 설정하는 속성은?**
① url
② type
③ data
④ success

**8. JSP의 백틱(```) 문법 내에서 EL(`${}`)과 자바스크립트 템플릿 리터럴의 충돌을 방지하기 위한 방법은?**
① `${}` 앞에 역슬래시(`\`)를 붙여 `\${}`로 표기한다.
② 모든 EL 태그를 삭제한다.
③ 백틱 대신 쌍따옴표만 사용한다.
④ `<c:out>` 태그로 감싼다.

**9. 다음 중 JSON(JavaScript Object Notation) 형식의 특징으로 틀린 것은?**
① 키(Key)와 값(Value)의 쌍으로 이루어져 있다.
② 자바의 VO(Value Object) 객체와 대응시키기 좋다.
③ 데이터 전송 시 XML보다 가볍고 가독성이 좋다.
④ 반드시 HTML 태그가 포함되어야 한다.

**10. `$.ajax()` 호출 시 `type: "POST"`로 설정했을 때 서버 Controller에서 받아야 하는 어노테이션은?**
① @GetMapping
② @PostMapping
③ @DeleteMapping
④ @PutMapping

**11. 서버에서 `new Person("홍길동", 25)` 객체를 `ResponseEntity`로 리턴했을 때, 클라이언트 success 함수 내에서 이름을 출력하는 방법은? (매개변수명은 `person`)**
① `person.getName()`
② `person["name"]`
③ `person.name`
④ `person.get("name")`

**12. MyBatis 환경에서 `@Mapper` 인터페이스의 역할은 무엇인가?**
① DB 연결 설정을 담당한다.
② 자바 메서드와 SQL 쿼리를 매핑해준다.
③ 결과를 HTML 화면으로 변환한다.
④ 클라이언트의 요청을 가장 먼저 받는 진입점이다.

**13. `A04_AjaxDBService` 클래스에 `@Service` 어노테이션을 붙이는 주된 이유는?**
① 컨트롤러의 기능을 대신하기 위해
② 비즈니스 로직을 수행하는 계층임을 명시하고 컨테이너에 빈으로 등록하기 위해
③ 데이터베이스 테이블을 직접 생성하기 위해
④ Ajax 요청을 직접 수신하기 위해

**14. Ajax 통신 중 `error: function(err){ ... }` 부분이 실행되는 상황이 아닌 것은?**
① 요청한 URL 주소가 서버에 존재하지 않을 때 (404)
② 서버 로직 수행 중 예외(Exception)가 발생했을 때 (500)
④ 서버에서 정상적으로 JSON 데이터를 반환했을 때 (200)
④ 네트워크 연결이 끊겼을 때

**15. Controller 메서드에서 `@RequestParam("name") String name`의 의미는?**
① 클라이언트가 보낸 쿼리 스트링 중 'name' 키의 값을 변수 name에 할당한다.
② 서버의 세션에 'name'이라는 값을 저장한다.
③ DB의 컬럼명 'name'을 자동으로 불러온다.
④ 응답 바디에 'name'이라는 문자열을 포함시킨다.

**16. 다음 중 부서 번호(deptno)를 변경할 때마다 Ajax를 호출하기 위해 적절한 jQuery 이벤트 핸들러는?**
① `.click()`
② `.hover()`
③ `.change()`
④ `.focus()`

**17. `$("#deptInfo").html(deptView)` 코드의 역할은 무엇인가?**
① `deptInfo`라는 id를 가진 요소의 내부 HTML을 `deptView` 내용으로 교체한다.
② `deptInfo` 뒤에 `deptView`를 추가한다.
③ `deptInfo` 요소를 화면에서 숨긴다.
④ 서버로 `deptView` 데이터를 전송한다.

**18. 객체 배열 형태의 데이터를 JSON으로 보낼 때 클라이언트에서 주로 사용하는 반복문 형태는?**
① `for(let i=0; ...)` 또는 `$.each()`
② `if ... else`
③ `switch ... case`
④ `try ... catch`

**19. `ResponseEntity<?>`에서 `<?>` 와일드카드의 의미는 무엇인가?**
① 반드시 Person 객체만 반환해야 한다.
② 어떤 타입의 객체든 응답 바디에 담아 반환할 수 있다.
③ 응답 코드만 보내고 데이터는 보내지 않는다.
④ 에러 메시지만 전송할 수 있다.

**20. Ajax를 이용한 CRUD 처리 시 '비동기(Asynchronous)'의 핵심 의미는?**
① 요청을 보낸 후 페이지 전체를 새로고침하여 데이터를 갱신한다.
② 요청을 보낸 후 응답이 올 때까지 브라우저의 모든 동작이 멈춘다.
③ 페이지 전체를 다시 로드하지 않고 필요한 부분의 데이터만 갱신한다.
④ 보안을 위해 데이터를 암호화하여 전송한다.

---

## II. 주관식 기술형 (5문제)

**1. `@ResponseBody`를 사용했을 때와 사용하지 않았을 때, Controller 메서드가 리턴하는 '문자열'의 의미 차이를 기술하세요.**

**2. Jackson 라이브러리(또는 Spring 내부 컨버터)가 자바 객체를 JSON으로 변환할 때, 자바 클래스(VO/DTO)에서 반드시 지켜야 할 구성 요소나 규칙에 대해 설명하세요.**

**3. 클라이언트가 `$.ajax`를 통해 서버에 데이터를 전달할 때, `data` 속성에 문자열 형식(`"key=value"`)과 객체 형식(`{key:value}`)을 사용하는 것의 차이점과 공통점을 기술하세요.**

**4. 서비스(Service) 계층과 DAO(Mapper) 계층을 분리하여 Ajax 요청을 처리하는 것이 유지보수 측면에서 왜 유리한지 기술하세요.**

**5. JSP 환경에서 Ajax를 구현할 때, 브라우저 콘솔(F12)의 'Network' 탭이 디버깅에 어떻게 활용될 수 있는지 그 용도를 기술하세요.**

