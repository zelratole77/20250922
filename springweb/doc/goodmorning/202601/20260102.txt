새해 복 많이 받으세요.^^  Good morning QUIZ!!

## I. 객관식 문제 (사지선다형)

**1. 파일 다운로드 처리 시 `ResponseEntity<Resource>`를 반환 타입으로 사용하는 주된 이유는 무엇입니까?**
① 별도의 JSP 뷰 페이지를 호출하기 위해서
② HTTP 응답 상태 코드, 헤더, 바디를 세밀하게 제어하기 위해서
③ 서버의 메모리 사용량을 최소화하기 위해서
④ 클라이언트의 자바스크립트 엔진을 직접 제어하기 위해서

**2. 파일 다운로드 시 `Content-Disposition` 헤더의 설정값 중 `attachment`가 의미하는 바는 무엇입니까?**
① 파일을 브라우저 화면 내에 직접 출력한다.
② 파일을 브라우저 캐시에 저장한다.
③ 파일을 저장할 수 있는 '다른 이름으로 저장' 대화상자를 띄운다.
④ 파일의 전송 속도를 최대한으로 설정한다.

**3. 한글 파일명을 네트워크 전송 시 `URLEncoder.encode()`를 사용하는 가장 큰 이유는 무엇입니까?**
① 파일의 용량을 압축하기 위해서
② 파일에 암호를 걸어 보안을 강화하기 위해서
③ 네트워크는 기본적으로 1바이트(ASCII) 단위로 전송되므로 비표준 문자의 깨짐을 방지하기 위해서
④ 파일의 확장자를 강제로 변경하기 위해서

**4. `Files.probeContentType(path)` 메서드의 역할은 무엇입니까?**
① 파일의 생성 날짜를 확인한다.
② 파일의 확장자를 기반으로 적절한 MIME 타입을 분석한다.
③ 파일이 바이러스에 감염되었는지 확인한다.
④ 파일의 실제 데이터 크기를 바이트 단위로 측정한다.

**5. 파일 다운로드 로직에서 `application/octet-stream`을 사용하는 시점은 언제입니까?**
① 파일의 형식이 이미지(JPG/PNG)일 때
② 파일의 MIME 타입을 명확히 알 수 없는 범용적인 이진 데이터일 때
③ 텍스트 파일만 전송할 때
④ 파일 업로드가 실패했을 때

**6. AJAX(Asynchronous Javascript and XML)의 '비동기적' 특성에 대한 설명으로 옳은 것은?**
① 데이터 요청 후 응답이 올 때까지 브라우저의 모든 동작이 멈춘다.
② 데이터를 주고받는 동안에도 사용자가 페이지의 다른 기능을 이용할 수 있다.
③ 모든 데이터 요청은 반드시 XML 형식으로만 이루어져야 한다.
④ 서버의 응답 속도가 항상 동기 방식보다 빠름을 보장한다.

**7. jQuery AJAX 속성 중 `dataType`이 수행하는 역할은 무엇입니까?**
① 서버로 보내는 데이터의 형식을 지정한다.
② 서버에서 받아온 응답 데이터를 어떤 형식(JSON, XML 등)으로 파싱할지 결정한다.
③ HTTP 요청 메서드(GET, POST)를 결정한다.
④ 서버의 응답 대기 시간을 설정한다.

**8. Spring Controller에서 AJAX 요청에 대해 뷰(JSP)를 거치지 않고 직접 데이터를 반환할 때 사용하는 어노테이션은?**
① @Controller
② @RequestMapping
③ @ResponseBody
④ @RequestParam

**9. `FileSystemResource` 객체가 파일 전송에서 담당하는 역할은 무엇입니까?**
① 파일의 경로를 DB에 저장한다.
② 파일 시스템의 물리적 경로에 있는 파일을 리소스로 추상화하여 전송 가능하게 한다.
③ 클라이언트의 다운로드 횟수를 제한한다.
④ 파일의 수정 권한을 관리한다.

**10. AJAX 통신 시 `success` 콜백 함수가 실행되는 조건은 무엇입니까?**
① 서버 호출 시 에러가 발생했을 때
② 서버가 HTTP 200번대(정상) 응답을 반환했을 때
③ 데이터 전송 속도가 기준치 이상일 때
④ 브라우저를 새로고침했을 때

**11. 다음 중 AJAX를 사용함으로써 얻는 이점이 아닌 것은?**
① 전체 페이지 새로고침 없는 부분 업데이트
② 서버로 전송되는 전체 트래픽 감소
③ 검색 엔진 최적화(SEO)의 비약적 상승
④ 향상된 사용자 경험(UX) 제공

**12. 다운로드 경로를 설정할 때 `@Value("${user.upload}")`와 같이 사용하는 이유는 무엇입니까?**
① 소스 코드 내에 경로를 하드코딩하지 않고 외부 설정 파일(properties)에서 관리하기 위해
② 자바 보안 기능을 활성화하기 위해
③ 파일의 용량을 제한하기 위해
④ DB 연결 정보를 가져오기 위해

**13. AJAX 요청에서 `type: "post"`를 사용할 때 주로 포함되는 속성은?**
① url
② data
③ contentType
④ 위 모두 포함될 수 있음

**14. 파일 다운로드 컨트롤러에서 `ResponseEntity.notFound().build()`는 어떤 상황에 실행됩니까?**
① 서버의 용량이 부족할 때
② 클라이언트가 요청한 경로에 파일이 존재하지 않을 때
③ 네트워크 연결이 끊겼을 때
④ 사용자의 권한이 없을 때

**15. JavaScript에서 특정 파일 리스트를 더블클릭(`ondblclick`)하여 다운로드를 실행할 때 적절한 구현 방식은?**
① `alert()` 창을 띄운다.
② `location.href`를 통해 다운로드 URL을 호출한다.
③ 페이지 전체를 `form submit` 한다.
④ 브라우저를 종료한다.

**16. AJAX 통신 중 서버 내부 오류(500 Error)가 발생했을 때 실행되는 jQuery 속성은?**
① success
② complete
③ error
④ beforeSend

**17. `Paths.get(path).resolve(fname).normalize()` 과정에서 `normalize()`의 역할은 무엇입니까?**
① 파일명을 대문자로 변경한다.
② 경로 내의 불필요한 요소(`.` 또는 `..`)를 제거하여 경로를 정규화한다.
③ 파일의 용량을 0으로 초기화한다.
④ 경로명을 암호화한다.

**18. `ResponseEntity.ok()` 메서드가 반환하는 HTTP 상태 코드는 무엇입니까?**
① 200
② 404
③ 500
④ 302

**19. AJAX를 활용한 실시간 검색 기능 구현 시 가장 적절한 데이터 형식은 무엇입니까?**
① HTML
② JSON
③ TXT
④ CSV

**20. 파일 다운로드 로직에서 `StandardCharsets.UTF_8`은 어떤 용도로 사용됩니까?**
① 파일 내부 데이터를 읽을 때
② 파일명을 인코딩할 때 사용할 문자 셋을 지정하기 위해
③ 서버의 시간대를 설정하기 위해
④ 데이터베이스의 암호화 알고리즘을 지정하기 위해

---

## II. 주관식 기술형 문제

**1. 파일 다운로드 시 `ResponseEntity`의 Body에 단순히 `String`이나 `int`가 아닌 `Resource` 객체를 담아 리턴해야 하는 기술적 이유를 설명하세요.**

**2. 클라이언트 단(JSP/JS)에서 파일 다운로드 시 AJAX를 사용하지 않고 `location.href`나 `<form>`을 사용하는 이유를 'Stream 전송' 관점에서 기술하세요.**

**3. Spring 환경에서 `@ResponseBody` 어노테이션이 붙은 메서드가 반환하는 데이터가 클라이언트에게 전달되기까지의 내부 처리 과정을 간단히 설명하세요.**

**4. 파일명을 네트워크 전송용으로 인코딩한 후 `.replaceAll("\\+", "%20")`을 수행하는 이유는 무엇인지 기술하세요.**

**5. AJAX 통신 시 `dataType`을 `"json"`으로 설정했을 때와 설정하지 않았을 때(default), `success` 함수로 들어오는 매개변수 데이터의 차이점을 설명하세요.**

