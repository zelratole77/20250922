1. [개념] Spring MVC 패턴에서 DAO(Data Access Object)를 연동하여 사용자 요청을 처리하는 전체적인 순서를 핵심 컴포넌트(Controller, Service, DAO, View)와 함께 기본 예시 코드를 들어 설명해 보세요.

2. [실습] 당신이 개발한 웹 서비스에서 사용자가 '게시글 작성' 버튼을 클릭하여 새로운 게시글 등록 요청을 보냈습니다. 이 요청이 데이터베이스에 저장되고 최종 응답이 사용자에게 전달될 때까지의 과정에서, **어떤 컴포넌트(Controller, Service, DAO)가 어떤 역할**을 수행하며 데이터를 주고받는지 순서대로 정리해 보세요.
		
3. [개념] Spring 기반 DAO 계층에서 SQL 쿼리 실행 결과가 정확히 하나의 값(예: 특정 상품의 가격, 총 회원 수)인 **단일 데이터**를 반환할 때, 개발자는 Java의 어떤 데이터 형식(Type)을 반환 타입으로 사용해야 하는지 기술하세요.

4. [실습] 고객 데이터베이스에 저장된 전체 고객의 평균 연봉(Average Salary)을 조회하는 `select avg(salary) from customers` 쿼리를 실행해야 합니다. 이 쿼리를 처리하기 위한 **DAO 메서드의 Java 시그니처**를 작성하세요. (단, 평균 연봉은 `double` 타입으로 가정합니다.)
   1) 사원의 최저급여 처리
   2) 사원번호 7521의 사원명
   3) 직책(job) CLERK의 평균 급여
5. [개념] Spring 기반 DAO 계층에서 SQL 쿼리 실행 결과가 **하나의 컬럼이지만 여러 행의 데이터**를 반환하는 경우(예: 모든 상품의 이름 목록, 모든 주문 번호 목록)에는 어떤 Java 컬렉션 및 제네릭 타입 조합을 반환 타입으로 사용하는 것이 가장 적절한지 기술하세요.

6. [실습] 회의 참석자 명단(이름만)을 데이터베이스에서 조회하는 `select attendee_name from meeting_attendees` 쿼리를 처리해야 합니다. 이 쿼리를 실행하여 참석자 이름(`String` 타입) 목록을 얻기 위한 **DAO 메서드의 Java 시그니처**를 작성하세요.
   1) DEPT의 위치(LOC)
   2) 직책(JOB)의 사원명(ENAME)

7. [개념] Spring DAO에서 **단일 요청 값(예: 직원 ID)을 받아 단일 데이터(예: 해당 직원 정보 전체)**를 조회하는 메서드를 구현할 때, 메서드 시그니처(반환 타입, 메서드명, 매개변수)를 어떤 방식으로 구성해야 하는지 구조적으로 설명하고, 이때 `SqlSessionTemplate` (또는 `JdbcTemplate`)에서 사용되는 핵심 메서드는 무엇인지 기술하세요.

8. [실습] 사용자 ID(`String userId`)를 매개변수로 받아 해당 사용자의 모든 정보가 담긴 `UserVO` 객체를 반환하는 DAO 메서드를 구현해야 합니다. `UserVO`는 해당 테이블의 모든 컬럼을 필드로 가지고 있는 객체입니다. 이 기능을 수행하는 **DAO 메서드의 전체적인 Java 코드 구조**를 주석과 함께 작성해 보세요. (실제 SQL 실행 코드는 생략하고 `TODO`로 표시합니다.)



